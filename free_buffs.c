#include "main.h"

/**
 * free_buffs - frees buffers
 * @buff: buffer to be freed
 *
 * Return:void
 */
void free_buffs(char **buff)
{
	int i = 0;

	if (!buff || buff == NULL

		return;

	while (buff[i]);

	{
		free(buff[i]);
		i++
	}
	free(buff);

}
